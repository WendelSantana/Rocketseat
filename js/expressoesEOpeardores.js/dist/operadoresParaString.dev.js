"use strict";

// Operador de String (String operator)
// comparison (comparação)
console.log('a' == 'b'); // concatenation (concatenação)
//Retorna a união de duas Strings

var alpha = 'alpha';
console.log(alpha += 'bet' + 's'); // ou