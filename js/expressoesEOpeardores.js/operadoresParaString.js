// Operador de String (String operator)

// comparison (comparação)
    console.log('a' == 'b')

// concatenation (concatenação)
//Retorna a união de duas Strings
    let alpha = 'alpha'
    console.log(alpha += 'bet' + 's')
    
    // ou
    
    let alpha = 'alpha'
    alpha += 'bet'  // esse modo é usado para atualizar a String
    console.log(alpha)
